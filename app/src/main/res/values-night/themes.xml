<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/accentColor</item>
        <item name="background">@color/backgroundColor</item>
        <item name="backgroundColor">@color/backgroundColor</item>
        <item name="rippleColor">@color/black</item>

        <item name="materialButtonStyle">@style/ButtonColor</item>

        <item name="android:textColor">@color/black</item>

        <item name="android:editTextColor">@color/black</item>
        
        <item name="textInputStyle">@style/TextInputStyle</item>
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="TextInputStyle" parent="Base.Theme.MyApplication">
        <item name="android:textColor">@color/black</item>
        <item name="passwordToggleTint">@color/hint</item>
    </style>

    <style name="ButtonColor" parent="ThemeOverlay.Material3.Button">
        <item name="backgroundTint">@color/accentColor</item>
        <item name="android:textColor">@color/white</item>
        <item name="rippleColor">@color/white</item>
        <item name="android:textAlignment">center</item>
        <item name="cornerRadius">14sp</item>
        <item name="android:paddingLeft">24sp</item>
        <item name="android:paddingRight">24sp</item>
        <item name="android:paddingTop">16sp</item>
        <item name="android:paddingBottom">16sp</item>
    </style>


    <style name="Theme.MyApplication.Splash" parent="@style/Theme.MyApplication">
        <item name="android:windowSplashScreenBackground">@color/accentColor</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_background</item>
    </style>
</resources>